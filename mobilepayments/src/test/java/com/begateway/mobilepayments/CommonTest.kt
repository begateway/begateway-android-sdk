package com.begateway.mobilepayments

import com.begateway.mobilepayments.model.TransactionType
import com.begateway.mobilepayments.model.network.request.Checkout
import com.google.gson.Gson
import com.google.gson.JsonElement
import com.google.gson.JsonObject
import org.junit.Test

class CommonTest {
    @Test
    fun abstractTest() {
//        val check = Checkout(2.1, true, TransactionType.AUTHORIZATION, 1, "sdsd",)
//        check.fields = arrayOf(JsonObject().apply { addProperty("prop", 4) })
//        val obj = Gson().toJson(check)
//        val obj2 = Gson().toJsonTree(check)
//        obj2.asJsonObject.addProperty("sdsds", 8)
//        println(obj)
    }
}